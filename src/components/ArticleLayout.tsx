import Link from "next/link"; import { ArrowLeft, Clock, Calendar, Tag } from "lucide-react"; interface ArticleLayoutProps { title: string; publishDate: string; readTime: string; tags?: string[]; children: React.ReactNode; } export default function ArticleLayout({ title, publishDate, readTime, tags = [], children, }: ArticleLayoutProps) { return ( <div className="min-h-screen bg-[var(--bg-secondary)]"> {/* Header */} <div className="border-b border-[var(--border)] bg-[var(--bg-primary)]"> <div className="max-w-4xl mx-auto px-4 py-6"> <Link href="/" className="inline-flex items-center gap-2 text-sm text-[var(--text-secondary)] hover:text-[var(--text-primary)] transition-colors mb-6" > <ArrowLeft size={16} /> Back to PM Streak </Link> <div className="flex flex-wrap items-center gap-4 text-sm text-[var(--text-secondary)] mb-4"> <span className="inline-flex items-center gap-1"> <Calendar size={14} /> {publishDate} </span> <span className="inline-flex items-center gap-1"> <Clock size={14} /> {readTime} read </span> </div> {tags.length > 0 && ( <div className="flex flex-wrap gap-2"> {tags.map((tag) => ( <span key={tag} className="inline-flex items-center gap-1 text-xs px-2.5 py-1 rounded-full bg-[var(--bg-secondary)] text-[var(--text-secondary)] border border-[var(--border)]" > <Tag size={10} /> {tag} </span> ))} </div> )} <h1 className="text-3xl md:text-4xl font-black text-[var(--text-primary)] mt-4 leading-tight"> {title} </h1> </div> </div> {/* Content */} <div className="max-w-4xl mx-auto px-4 py-10"> <div className="bg-[var(--bg-primary)] rounded-2xl border border-[var(--border)] p-6 md:p-10"> {children} </div> </div> </div> ); }